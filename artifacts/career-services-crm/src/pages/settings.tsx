import { Check, ChevronRight, CircleUserRound, Mail, Moon, PlugZap, Save, ShieldCheck } from 'lucide-react';
import { useState } from 'react';
import { AppShell } from '@/components/app-shell';
import { PageHeader } from '@/components/page-primitives';

export default function Settings() {
  const [name, setName] = useState('Naveed');
  const [email, setEmail] = useState('maya.chen@harbor.edu');
  const [saved, setSaved] = useState(false);
  const [compact, setCompact] = useState(true);
  const save = (event: React.FormEvent) => { event.preventDefault(); setSaved(true); window.setTimeout(() => setSaved(false), 2200); };
  return <AppShell><div className="mx-auto max-w-[1100px] px-5 py-7 md:px-9 md:py-9"><PageHeader eyebrow="Workspace preferences" title="Settings" description="A few quiet controls for the way you work. This workspace is private to you." /><div className="grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><div className="space-y-5"><section className="rounded-[14px] border border-border bg-card"><div className="border-b border-border px-6 py-5"><div className="flex items-center gap-3"><div className="rounded-lg bg-secondary p-2 text-primary"><CircleUserRound className="h-4 w-4" /></div><div><h2 className="font-serif text-lg font-bold">Your profile</h2><p className="mt-0.5 text-xs text-muted-foreground">Shown only inside this workspace.</p></div></div></div><form onSubmit={save} className="space-y-4 p-6"><div className="grid gap-4 sm:grid-cols-2"><label className="space-y-1.5 text-xs font-bold"><span>Your name</span><input value={name} onChange={(event) => setName(event.target.value)} className="h-10 w-full rounded-lg border border-input bg-background px-3 text-sm font-normal outline-none focus:border-accent" data-testid="input-settings-name" /></label><label className="space-y-1.5 text-xs font-bold"><span>Work email</span><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="h-10 w-full rounded-lg border border-input bg-background px-3 text-sm font-normal outline-none focus:border-accent" data-testid="input-settings-email" /></label></div><div className="flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted-foreground">{saved ? <span className="flex items-center gap-1.5 font-semibold text-[#287668]"><Check className="h-3.5 w-3.5" />Saved just now</span> : 'Changes are saved to this browser.'}</span><button type="submit" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground hover:bg-primary/90" data-testid="button-save-settings"><Save className="h-3.5 w-3.5" />Save profile</button></div></form></section><section className="rounded-[14px] border border-border bg-card"><div className="border-b border-border px-6 py-5"><div className="flex items-center gap-3"><div className="rounded-lg bg-[#f8e9dc] p-2 text-[#a75d31]"><Mail className="h-4 w-4" /></div><div><h2 className="font-serif text-lg font-bold">Outlook connection</h2><p className="mt-0.5 text-xs text-muted-foreground">Connect email later to keep employer follow-ups close at hand.</p></div></div></div><div className="flex flex-col gap-4 p-6 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-muted"><PlugZap className="h-4 w-4 text-muted-foreground" /></span><div><div className="text-sm font-bold">Not connected</div><div className="mt-1 text-xs text-muted-foreground">Outlook sync is not enabled for this workspace.</div></div></div><button type="button" onClick={() => window.alert('Outlook connection will be available when workspace sync is enabled.')} className="inline-flex items-center justify-center gap-2 rounded-lg border border-border px-3.5 py-2.5 text-xs font-bold hover:border-accent hover:bg-accent/10" data-testid="button-connect-outlook">Connect Outlook <ChevronRight className="h-3.5 w-3.5" /></button></div></section></div><div className="space-y-5"><section className="rounded-[14px] border border-border bg-card p-6"><div className="flex items-center gap-3"><div className="rounded-lg bg-[#e3f2ee] p-2 text-[#287668]"><ShieldCheck className="h-4 w-4" /></div><div><h2 className="font-serif text-lg font-bold">Privacy first</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Graduate visibility is respected in every matching view. Private records never appear in share suggestions.</p></div></div><div className="mt-5 border-t border-border pt-4 text-[11px] leading-5 text-muted-foreground">Your workspace contains sensitive career information. Keep access limited to your own account and review consent regularly.</div></section><section className="rounded-[14px] border border-border bg-card p-6"><div className="flex items-center justify-between"><div><h2 className="font-serif text-lg font-bold">Display density</h2><p className="mt-1 text-xs text-muted-foreground">Keep more rows in view while you work.</p></div><button type="button" onClick={() => setCompact(!compact)} className={`relative h-6 w-11 rounded-full ${compact ? 'bg-accent' : 'bg-muted-foreground/30'}`} data-testid="button-toggle-density" aria-label="Toggle compact density"><span className={`absolute top-1 h-4 w-4 rounded-full bg-card shadow-sm ${compact ? 'left-6' : 'left-1'}`} /></button></div><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><div className="flex items-center gap-2 text-xs font-semibold"><Moon className="h-3.5 w-3.5 text-muted-foreground" />Light workspace</div><span className="rounded-full bg-secondary px-2 py-1 text-[10px] font-bold text-muted-foreground">Default</span></div></section></div></div></div></AppShell>;
}